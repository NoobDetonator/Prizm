{
  "name": "prizm",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/test-dispersion.mjs",
    "test:dispersion": "node scripts/test-dispersion.mjs",
    "test:leak": "node scripts/leak-test.mjs",
    "check:lib": "node scripts/check-lib-bundle.mjs",
    "audit:sliders": "node scripts/slider-audit-after.mjs",
    "capture:previews": "node scripts/capture-previews.mjs",
    "capture:matrix": "node scripts/capture-matrix.mjs",
    "lint:dead": "node -e \"console.log('Run: npx knip (optional). Rule: no V2/New/Final suffixes; delete replaced files in the same commit.')\""
  },
  "dependencies": {
    "three": "^0.178.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
